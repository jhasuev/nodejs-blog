{{>head }}

{{#if authed}}
  {{>header-authed }}
{{else}}
  {{>header }}
{{/if}}

<main class="container main py-4">
  <div class="row">
    <div class="col-lg-8 col-xl-9 content">
      {{{ body }}}
    </div>

    <sidebar class="col-lg-4 col-xl-3 sidebar">
      {{> categories }}
      {{> recent-posts }}
      {{> popular-posts }}
    </sidebar>
  </div>
</main>

{{> footer }}