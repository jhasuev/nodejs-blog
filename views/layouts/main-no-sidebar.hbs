{{>head }}

{{#if authed}}
  {{>header-authed }}
{{else}}
  {{>header }}
{{/if}}

<main class="container main py-4">
  <div class="row">
    <div class="col-12 content">
      {{{ body }}}
    </div>
  </div>
</main>

{{> footer }}